server:
    port: 8080
spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/my_pgdb
        username: postgres
        password: postgres
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true

